apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../upstream/tekton-dashboard-0.72.0.yaml

patchesStrategicMerge:
  - patches/remove-rbac.yaml
  - patches/deployment.yaml